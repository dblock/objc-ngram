language: objective-c
before_script:
before_script:
  - brew update
  - brew uninstall xctool
  - brew install xctool
  - export LANG=en_US.UTF-8
  - gem i cocoapods --no-ri --no-rdoc
  - pod install
xcode_workspace: objc-ngram.workspace
xcode_scheme: Tests
xcode_sdk: iphonesimulator
